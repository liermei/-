10.1
#include<iostream>
#include<algorithm>
#include<vector>
#include<ctime>
#include<cstdlib>
using namespace std;
int main()
{
	srand((unsigned)time(NULL));
	vector<int>arr;
	for (int i = 0; i < 100; i++)
		arr.push_back(rand() % 10);
	cout << "请输入1-10以内的整数" << endl;
	int val;
	while(!(cin>>val))                ！！ //注意这段代码
	{
		cerr << "输入错误" << endl;          
		cin.clear();//重置输入流         //首先因为cin已经处于错误状态，无法读取了，因此需要让它恢复能读取的状态。
		while (cin.get() != '\n')            //然后把输入流中的东西都读取掉。
			continue;                         //现在Cin输入流为空了，用户可以继续输入。
		cout << "请输入1-10以内的整数" << endl;
	}
	int i = count(arr.begin(), arr.end(), val);
		cout << val << "一共出现了" << i << "次" << endl;
	system("pause");
	return 0;
}

